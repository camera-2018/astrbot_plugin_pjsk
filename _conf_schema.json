{
    "pjsk_req_retry": {
        "description": "请求重试次数",
        "type": "int",
        "default": 1,
        "hint": "下载资源失败时的重试次数"
    },
    "pjsk_req_timeout": {
        "description": "请求超时时间",
        "type": "int",
        "default": 10,
        "hint": "下载资源的超时时间（秒）"
    },
    "pjsk_use_cache": {
        "description": "使用缓存",
        "type": "bool",
        "default": true,
        "hint": "是否缓存生成的表情图片"
    },
    "pjsk_clear_cache": {
        "description": "清理缓存",
        "type": "bool",
        "default": false,
        "hint": "启动时是否清理所有缓存"
    },
    "pjsk_playwright_auto_install": {
        "description": "自动安装 Playwright 浏览器",
        "type": "bool",
        "default": true,
        "hint": "启动时检测到 Chromium 运行文件缺失时，自动执行 playwright install"
    },
    "pjsk_playwright_install_timeout": {
        "description": "Playwright 安装超时",
        "type": "int",
        "default": 300,
        "hint": "自动安装 Playwright Chromium 的最长等待时间（秒），最低 30 秒"
    }
}
